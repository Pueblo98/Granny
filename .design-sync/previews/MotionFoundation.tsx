import * as React from 'react';
import { MotionFoundation } from '@granny/design-system';

/** Durations, easing and the reduced-motion contract. */
export const Durations = () => <MotionFoundation />;
