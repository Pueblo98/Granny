import * as React from 'react';
import { TargetFoundation } from '@granny/design-system';

/** Target floors and focus geometry, drawn at real size. */
export const Geometry = () => <TargetFoundation />;
