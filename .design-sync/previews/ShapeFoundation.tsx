import * as React from 'react';
import { ShapeFoundation } from '@granny/design-system';

/** Open Day radii — softer: 12dp controls, 16dp containers. */
export const OpenDay = () => <ShapeFoundation theme="open-day" />;

/** Bright Signal radii — crisper: 8dp throughout. Identical role names. */
export const BrightSignal = () => <ShapeFoundation theme="bright-signal" />;
